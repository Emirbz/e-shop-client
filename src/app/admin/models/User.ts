import Picture from './Picture';

export default class User {
  id?: string;
  firstName?: string;
  lastName: string;
  email?: string;
  address?: string;
  town: string;
  postalCode: string;
  phone: string;
}
